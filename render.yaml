services:
  - type: web
    name: pacificorp-mmm
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "streamlit run src/app.py --server.port 10000 --server.address 0.0.0.0"
